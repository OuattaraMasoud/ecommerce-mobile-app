import 'package:flutter/material.dart';

class OnSaleScreen extends StatelessWidget {
  static const routeName = 'OnSaleScreenView';

  const OnSaleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
